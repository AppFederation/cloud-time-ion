{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": [
      "chrome"
    ],
    // ======= Strictness / checks:
    "strictNullChecks": true,
    "noImplicitAny" : true,
    "strict": true
  },
  "files": [
    "main.ts",
    "polyfills.ts",
    "zone-flags.ts",
//    "background/background.ts" /* disabled coz causes warning when running; and I dont work on extension now */
  ],
  "include": [
    "src/**/*.d.ts"
  ]
}
