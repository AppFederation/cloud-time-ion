import { OdmListItemDirective } from './odm-list-item.directive';

describe('OdmListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new OdmListItemDirective();
    expect(directive).toBeTruthy();
  });
});
