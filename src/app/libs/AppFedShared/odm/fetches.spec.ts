// write quick test reflecting sync statuses from different sources;
// - local,
// - "Last 10 days",
// - "All" - definite: true
// (or order decides)

// do not assume the order in which they finish

// firing a firestore query, could add create 2 source-fetches - 1 local and 1 remote
// - when query callback fires, the system would mark local/remote as finished

//

// and for given OdmList - with parentId - "fetching sub-items of <some Item, e.g. category x>"


// describe()
// it("handles multiple source-fetches")

// it("indicates no fetching in progress, when all fetches finish")
