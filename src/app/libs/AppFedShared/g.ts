import {FeatureService} from './feature.service'
import {FeaturesConfig} from './FeaturesConfig'

export const g = {
} as {
  feat: FeaturesConfig
}

/** easier to find in IDE */
export const appGlobals = g
