import { QuizIntervalCalculator } from './quiz-interval-calculator';

describe('QuizIntervalCalculator', () => {
  it('should create an instance', () => {
    expect(new QuizIntervalCalculator()).toBeTruthy();
  });
});
