import { createAction } from '@ngrx/store';

export const requestNextQuizItem = createAction('[Quiz] Next Item Requested');
export const requestNextQuizItemSuccess = createAction('[Quiz] Next Item Success');
